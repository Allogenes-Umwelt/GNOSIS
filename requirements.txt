pandas
openpyxl
numpy
tabula-py
# PyPDF2 1.26.0 arrastra PYSEC-2022-194 y PYSEC-2026-1837 (fix en 1.27.x) y
# ademas esta deprecado en favor de pypdf. NO se sube aqui: solo lo importan
# el pipelegado (PDFs_v2, PDFs_Final_v3, Estadistico) y app.py en su ruta de
# extraccion, calibrados al formato exacto de las facturas VW y sin fixtures
# reales en este entorno. Cambiarlo a ciegas violaria ZERO SNAKE OIL.
# Decision del operador, con PDFs reales delante.
PyPDF2==1.26.0
# Flask/Werkzeug: subidos desde 2.0.3 (15 CVEs conocidos entre ambos,
# pip-audit 2026-09-02). La superficie usada es minima y estable en el
# salto 2->3: HTTPException, secure_filename, Blueprint, render_template,
# request, jsonify, current_app. Ninguna API retirada en 3.x se usa.
# Mantener en sintonia con .github/workflows/ci.yml: CI debe probar el
# stack que la imagen instala, no el ultimo que pip encuentre.
Flask==3.1.3
Werkzeug==3.1.8
jpype1
pdfplumber
pytesseract
pdf2image
Pillow
pdfminer.six
camelot-py
pywaffle
bokeh
plotly
anthropic>=0.39.0
python-dotenv
pydantic>=2.7
gunicorn>=22.0
networkx>=3.2
requests
xlrd
